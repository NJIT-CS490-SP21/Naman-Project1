# Naman Project 1 Demo

## Set up
1. `pip install requests`
2. Add `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET`, `GENIUS_ACCESS_TOKEN` to `keys.env` file
3. Run `source keys.env` 
4. Run `app.py`
5. Preview app in browser
